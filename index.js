let tables=require('./create-table')
let Enteries=require('./insert-data')
// tables();
// Enteries();  //run these two once and then comment them as if these are run multiple times then mutplie enteries and tables will be created